# Implementation

Capture the local static dashboard with Python Playwright at native 2x pixel density.
Use a scheduled GitHub workflow to commit only the image. Keep the existing public data contract.
Change browser polling and both README captions to ten minutes. Validate capture, existing tests,
and diff. Preserve the prior unpushed profile changes.
