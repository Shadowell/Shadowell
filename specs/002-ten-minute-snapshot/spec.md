# Ten-minute Paper snapshot

Update the README image on a ten-minute schedule and poll the interactive page every ten minutes.
Expose capture time and warn that GitHub scheduling and image caching can delay visibility.
Use only the existing public alias; render unavailable state when data cannot be fetched.
Do not alter Paper configuration. Publication requires pushing the workflow to the default branch.
